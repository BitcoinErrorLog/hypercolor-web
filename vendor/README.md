# DO NOT USE THIS WHEN BUILDING.

Everything under `vendor/` is retained only as an immutable historical
artifact for provenance. It is not part of the terminally unavailable
production application and must not be imported, initialized, executed, or
used to produce a build.
