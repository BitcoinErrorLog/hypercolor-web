# Security posture

Hypercolor is terminally unavailable. No production route initializes a
session, loads a network client, accesses storage, restores state, or runs
product code. There is no account, handoff, backup, migration, compatibility,
fallback, or recovery path.

Only the terminal unavailable page and its route coverage are retained.
